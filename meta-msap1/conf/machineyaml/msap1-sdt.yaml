inherit: ${LAYER_PATH_kria-layer}/conf/machineyaml/k26-smk-kr-sdt.yaml

# Merge MSAP1 Linux ownership overrides into the generated full PL overlay.
# gen-machineconf passes this input to the xlnx_overlay_pl_dt lopper assist.
kconfig:
    CONFIG_SUBSYSTEM_PL_INPUT_DTSI: "${MSAP1_LAYERDIR}/conf/dtsi/msap1-ad7771-iio.dtsi"
