# MSAP1 optional PTP mode requires the ZynqMP Cadence GEM hardware clock and
# packet timestamp path. Pin these product dependencies instead of relying on
# the inherited Xilinx BSP defaults.
CONFIG_MACB=y
CONFIG_PTP_1588_CLOCK=y
CONFIG_MACB_USE_HWSTAMP=y
